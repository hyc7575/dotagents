# AGENTS.md

모든 코딩 에이전트(Claude Code, Codex CLI, Cursor 등)가 따라야 할 공통 행동 원칙.
프로젝트별 규칙(빌드 명령, 디렉토리 구조, 스택 가이드)은 이 파일 아래에 섹션으로 추가한다.

## 소통

- 한국어로 소통. 기술 용어는 영어 유지.
- 모호하면 가정하지 말고 질문한다. 추측한 부분은 명시적으로 표면화한다.
- 트레이드오프가 있는 결정은 옵션을 제시한다. 임의로 하나를 선택해 진행하지 않는다.
- 확신이 없으면 "확실하지 않다"고 말한다. 그럴듯하게 지어내지 않는다.

## 변경 범위

- 요청받은 것만 수정한다. 요청받지 않은 코드/주석/포맷/import는 건드리지 않는다.
- 이해하지 못한 코드는 손대지 말고 먼저 질문한다.
- 변경의 blast radius를 최소화한다. 한 파일로 해결되면 한 파일만 본다.
- 리팩토링과 기능 변경을 한 커밋에 섞지 않는다.

## 코드 작성

- 100줄로 될 일을 1000줄로 만들지 않는다. 단순함이 기본값.
- 추상화는 두 번째 케이스가 나타날 때 추출한다. 미리 만들지 않는다.
- 중복 제거는 우연한 유사성이 아니라 같은 개념일 때만 한다.
- 기존 코드의 패턴/네이밍/구조를 먼저 확인하고 따른다. 새 패턴을 도입할 때는 이유를 명시한다.
- 죽은 코드, 사용하지 않는 import, 콘솔 로그를 남기지 않는다.

## 작업 디시플린

작업 흐름을 강제하는 프레임워크(Superpowers, gstack, GSD 등)가 활성화돼 있으면 그 워크플로우를 우선한다.

활성화된 프레임워크가 없을 때는 다음을 직접 수행한다:

- 작업 시작 전: 무엇을 만들지, 어떤 파일을 건드릴지, 어떻게 검증할지 한 문단으로 정리한다.
- 작업 종료 전: 변경이 의도대로 작동하는지 명시적으로 검증한다 (테스트 실행, 빌드 통과, 또는 명확한 success criteria 충족).
- 검증 없이 "완료"라고 말하지 않는다.

## 실패 처리

- 실패하면 숨기지 말고 보고한다. 실패한 이유와 다음 시도를 명시한다.
- 같은 방식으로 3번 이상 실패하면 멈추고 접근 방식을 다시 논의한다.

## Git

- `--no-verify` 절대 금지.
- 커밋 메시지는 *왜*를 설명한다. *무엇*은 diff에 이미 있다.
- 한 커밋 = 한 논리적 변경.
